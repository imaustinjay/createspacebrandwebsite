{
  "name": "createspace-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "createspace \u00b7 community + talent \u2014 public marketing site (createspacebrand.com)",
  "scripts": {
    "dev": "npx -y serve public",
    "dev:functions": "npx -y netlify-cli dev",
    "test": "node --test netlify/shared/*.test.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.9",
    "nodemailer": "^9.0.1",
    "stripe": "^22.5.0"
  }
}
